// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc {{.SqlcVersion}}

package {{.Package}};

{{range imports .SourceName}}
{{range .}}import {{.}};
{{end}}
{{end}}

public final class Models {

    private Models() {}

    {{range .Enums}}
    {{if .Comment}}{{comment .Comment}}{{end}}
    public static enum {{.Name}} {
        {{- range $i, $e := .Constants}}
        {{- if $i }},{{end}}
        {{.Name}}("{{.Value}}")
        {{- end}};

        private static final Map<String, {{.Name}}> BY_VALUE;

        static {
            for ({{.Name}} v: values()) {
                BY_LABEL.put(v.value, v);
            }
        }

        private final String value;

        private {{.Name}}(final String value) {
            this.value = value;
        }

        public static {{.Name}} lookup(final String value) {
            return BY_VALUE.get(value);
        }

        public String value() {
            return value;
        }
    }
    {{end}}

    {{range .DataClasses}}
    {{if .Comment}}{{comment .Comment}}{{end}}
    public static record {{.Name}}( {{- range $i, $e := .Fields}}
            {{- if $i }},{{end}}
            {{- if .Comment}}
            {{comment .Comment}}{{else}}
            {{- end}}
            {{- if $i }},{{end}}
            {{.Type}} {{.Name}}
            {{- end}}
            {{- end}}) {}
    {{end}}
}